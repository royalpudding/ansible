- hosts: web
  vars:
    http_port: 80
    max_clients: 200
  remote_user: ansible
  become: yes
  pre_tasks:
    - name: Include Variables
      include_vars:
        dir: sites/school
        name: sites
  handlers:
    - import_tasks: handler.yml

  roles:
    - deploy


    

    #